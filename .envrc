# direnv: auto-load the Nix dev shell on `cd` into this repo.
# Requires direnv (https://direnv.net) + nix-direnv. Run `direnv allow` once.
use flake

# Load the project's .env (kafka/redis/service ports) into the shell too.
dotenv_if_exists .env
